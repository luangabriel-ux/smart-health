{
  "name": "smart-health",
  "version": "0.1.0",
  "private": true,
  "description": "Estrutura inicial do Smart Health — somente ambiente e pastas.",
  "workspaces": [
    "backend",
    "frontend"
  ],
  "engines": {
    "node": ">=22.13.0",
    "npm": ">=10"
  },
  "scripts": {
    "db:up": "docker compose up -d --wait database",
    "db:down": "docker compose down"
  }
}
